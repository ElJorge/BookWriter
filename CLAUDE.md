# BookWriter

## Overview
Node.js project (early scaffold stage). Entry point is `index.js`. As the project
grows, keep this file updated so Claude Code has accurate context.

## Stack
- Node.js v20.x (ESM/CJS — currently CommonJS via `index.js`)
- npm for package management

## Commands
- `npm test` — test runner (not yet configured; currently a placeholder)
- `node index.js` — run the entry point

## Conventions
- Keep new code consistent with surrounding style.
- Update the **Commands** and **Stack** sections here whenever build/test/run
  scripts or major dependencies change.

## Notes for Claude
- This is a fresh repo with no commits yet. Confirm intent before large scaffolds.
- Prefer small, reviewable changes.
